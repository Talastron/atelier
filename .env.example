# Copy this file to .env.local and fill in your values.
# .env.local is gitignored — never commit it.

# ─── Firebase ──────────────────────────────────────────────────────────────
# 1. Create a Firebase project at https://console.firebase.google.com
# 2. Enable Authentication → Sign-in method → Google
# 3. Enable Firestore Database (Native mode)
# 4. Project settings → General → Your apps → Add web app → copy the config
#
# These VITE_* values are baked into the public client bundle — they are NOT
# secrets. Security comes from your Firestore rules, not from hiding keys.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# ─── Owners ────────────────────────────────────────────────────────────────
# Comma-separated list of Google account emails with owner-level access.
# Owners can invite/revoke other users from inside the app.
# MUST match the email list in firestore.rules (search "EDIT THIS LIST").
VITE_OWNER_EMAILS=you@example.com

# ─── Gemini (optional, recommended) ────────────────────────────────────────
# Powers all AI features: Identify-with-AI, Scan Label, Receipt OCR, Style
# Manifesto, Gap Analysis, Today suggestions, Travel packing, Inspiration
# analysis. Free tier from https://aistudio.google.com/app/apikey is generous.
# The app works without this — AI features simply won't render.
VITE_GEMINI_API_KEY=
