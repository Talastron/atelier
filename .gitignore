# dependencies
node_modules/
.pnpm-store/

# build outputs
dist/
.astro/
.wrangler/
.vercel/

# env
.env
.env.local
.env.*.local
.dev.vars

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# isolated worktrees, created inside the repo by the agent harness. Registered
# worktrees stay out of `git status` on their own, but nothing stops a stray
# `git add -A` from committing one whole checkout into another.
.claude/worktrees/

# editor
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
