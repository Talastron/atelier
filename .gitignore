node_modules/
dist/
dist-ssr/

# Local config — kept out of git so forks aren't tied to one Firebase project
# or one personal account. See *.example files for the templates.
.env
.env.local
.env.*.local
.firebaserc
.firebase/
firestore.rules

.DS_Store
*.log
.vite/
.cache/
.idea/
.vscode/

# Local Claude Code session state — contains debug tokens, agent transcripts
.claude/

# Generated build artifact, not source
prod.html
